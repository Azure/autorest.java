// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.multiinterfaceclient.implementation;

import com.azure.core.http.HttpPipeline;
import com.azure.core.http.HttpPipelineBuilder;
import com.azure.core.http.policy.CookiePolicy;
import com.azure.core.http.policy.RetryPolicy;
import com.azure.core.http.policy.UserAgentPolicy;
import com.azure.core.util.serializer.JacksonAdapter;
import com.azure.core.util.serializer.SerializerAdapter;

/** Initializes a new instance of the MultiInterfaceClient type. */
public final class MultiInterfaceClientImpl {
    /** The HTTP pipeline to send requests through. */
    private final HttpPipeline httpPipeline;

    /**
     * Gets The HTTP pipeline to send requests through.
     *
     * @return the httpPipeline value.
     */
    public HttpPipeline getHttpPipeline() {
        return this.httpPipeline;
    }

    /** The serializer to serialize an object into a string. */
    private final SerializerAdapter serializerAdapter;

    /**
     * Gets The serializer to serialize an object into a string.
     *
     * @return the serializerAdapter value.
     */
    public SerializerAdapter getSerializerAdapter() {
        return this.serializerAdapter;
    }

    /** The DogsImpl object to access its operations. */
    private final DogsImpl dogs;

    /**
     * Gets the DogsImpl object to access its operations.
     *
     * @return the DogsImpl object.
     */
    public DogsImpl getDogs() {
        return this.dogs;
    }

    /** The CatsImpl object to access its operations. */
    private final CatsImpl cats;

    /**
     * Gets the CatsImpl object to access its operations.
     *
     * @return the CatsImpl object.
     */
    public CatsImpl getCats() {
        return this.cats;
    }

    /** Initializes an instance of MultiInterfaceClient client. */
    public MultiInterfaceClientImpl() {
        this(
                new HttpPipelineBuilder()
                        .policies(new UserAgentPolicy(), new RetryPolicy(), new CookiePolicy())
                        .build(),
                JacksonAdapter.createDefaultSerializerAdapter());
    }

    /**
     * Initializes an instance of MultiInterfaceClient client.
     *
     * @param httpPipeline The HTTP pipeline to send requests through.
     */
    public MultiInterfaceClientImpl(HttpPipeline httpPipeline) {
        this(httpPipeline, JacksonAdapter.createDefaultSerializerAdapter());
    }

    /**
     * Initializes an instance of MultiInterfaceClient client.
     *
     * @param httpPipeline The HTTP pipeline to send requests through.
     * @param serializerAdapter The serializer to serialize an object into a string.
     */
    public MultiInterfaceClientImpl(HttpPipeline httpPipeline, SerializerAdapter serializerAdapter) {
        this.httpPipeline = httpPipeline;
        this.serializerAdapter = serializerAdapter;
        this.dogs = new DogsImpl(this);
        this.cats = new CatsImpl(this);
    }
}
